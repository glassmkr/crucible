---
name: Feature Request
about: Propose a new collector, alert rule, or capability
---

**What problem would this solve?**
(the operational situation, not the implementation; e.g. "I cannot tell when
a RAID resync is running" beats "add a resync field")

**What signal or data source would it read?**
(e.g. a file under /sys, a tool like `mdadm --detail`, an IPMI sensor; if you
have example output from real hardware, paste it here; real output is the
single most useful thing you can give us)

**Environment where you would use it:**
- OS/distro:
- Hardware (vendor/model, controller, drive types if relevant):

**Anything you have tried as a workaround:**
